// AVH V3 — Autenticación y cambio obligatorio de contraseña.
async function signIn(user,password){const email=user.includes('@')?user:`${user}@avh.local`;const r=await request('/auth/v1/token?grant_type=password',{method:'POST',body:{email,password}});if(r.error)return r;saveSession({...r.data,expires_at:Date.now()+Number(r.data.expires_in||3600)*1000});return r}
async function signOut(){const old=session;saveSession(null);profile=null;clearInterval(refreshTimer);showApp(false);$('#loginPass').value='';try{if(old?.access_token){session=old;await request('/auth/v1/logout',{method:'POST'},false)}}catch{}session=null;location.reload()}
function forcePassword(){showApp(false);$('#login').classList.remove('hide');$('#login').innerHTML=`<div class="login-card"><div class="login-logo">AVH</div><h1>Cambiar contraseña</h1><p>Es tu primer ingreso. Elegí una contraseña propia.</p><form id="pwForm"><div class="field"><label>Nueva contraseña</label><input id="pw1" type="password"></div><div class="field"><label>Repetir contraseña</label><input id="pw2" type="password"></div><div id="pwMsg"></div><button class="btn primary" style="width:100%">Guardar</button></form></div>`;$('#pwForm').onsubmit=async e=>{e.preventDefault();const a=$('#pw1').value,b=$('#pw2').value;if(a!==b||a.length<10||!/[A-Z]/.test(a)||!/[a-z]/.test(a)||!/[0-9]/.test(a))return msg($('#pwMsg'),'Mínimo 10 caracteres, con mayúscula, minúscula y número.');let r=await request('/auth/v1/user',{method:'PUT',body:{password:a}});if(r.error)return msg($('#pwMsg'),r.error);r=await rpc('complete_password_change');if(r.error)return msg($('#pwMsg'),r.error);location.reload()}}
