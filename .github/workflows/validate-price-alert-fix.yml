name: Validate price alert fix

on:
  push:
    branches:
      - fix/price-alert-priority

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Validate AVH V3
        working-directory: v3
        run: node scripts/check.mjs
