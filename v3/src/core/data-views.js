// AVH V3 — Carga de datos y vistas principales.
async function safeLoad(key,promise){const r=await promise;if(!r.error)D[key]=r.data||[];return r.error}
async function loadAll(force=false){if(loading)return;loading=true;const jobs=[safeLoad('warehouses',query('warehouses','*','order=name.asc')),safeLoad('products',query('products','*','order=name.asc')),safeLoad('presentations',query('product_presentations','*','order=label.asc')),safeLoad('contractors',query('contractors','*','order=name.asc')),safeLoad('barges',query('barges','*','order=number.asc')),safeLoad('suppliers',query('suppliers','*','order=name.asc')),safeLoad('stocks',query('v_stock_by_warehouse')),safeLoad('stockStatus',query('v_stock_status')),safeLoad('stockValues',query('v_stock_value_by_warehouse')),safeLoad('minimums',query('stock_minimums')),safeLoad('moves',query('movements','*,movement_lines(*,products(name,base_unit))','order=created_at.desc&limit=400')),safeLoad('bargeConsumption',query('v_barge_consumption','*','order=barge_number.asc')),safeLoad('contractorConsumption',query('v_contractor_consumption','*','order=contractor_name.asc')),safeLoad('productConsumption',query('v_product_consumption','*','order=base_quantity.desc')),safeLoad('warehouseActivity',query('v_warehouse_activity_today'))];if(profile.role==='admin'){jobs.push(safeLoad('productRequests',query('product_requests','*','order=created_at.desc&limit=100')),safeLoad('correctionRequests',query('correction_requests','*','order=created_at.desc&limit=100')),safeLoad('profiles',query('profiles','*','order=username.asc')))}jobs.push(safeLoad('notifications',query('notifications','*','order=created_at.desc&limit=80')));await Promise.all(jobs);loading=false;renderAll()}
function renderAll(){renderSelectors();renderHome();renderStock();renderMoves();renderBarges();if(activeModule)renderModule(activeModule)}
function renderSelectors(){const whOpt=`<option value="all">Todos los depósitos</option>`+D.warehouses.filter(x=>x.active).map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('');for(const id of ['stockWarehouse','moveWarehouse']){const el=$('#'+id),old=el.value;el.innerHTML=whOpt;if(old)el.value=old}const b=$('#moveBarge'),bo=b.value;b.innerHTML='<option value="all">Todas las barcazas</option>'+D.barges.filter(x=>x.active).map(x=>`<option value="${x.id}">Barcaza ${x.number}</option>`).join('');if(bo)b.value=bo;const c=$('#moveContractor'),co=c.value;c.innerHTML='<option value="all">Todos los contratistas</option>'+D.contractors.filter(x=>x.active).map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('');if(co)c.value=co}
function criticalRows(){return D.stockStatus.filter(x=>x.is_critical)}
function kpiValue(currency){return D.stockValues.filter(x=>x.currency===currency).reduce((a,x)=>a+Number(x.stock_value||0),0)}
function stockDataset(){const map=new Map;D.stocks.forEach(x=>map.set(`${x.warehouse_id}|${x.product_id}`,{...x,minimum_qty:null,is_critical:false}));D.stockStatus.forEach(x=>{const k=`${x.warehouse_id}|${x.product_id}`;map.set(k,{...(map.get(k)||x),...x})});return [...map.values()]}
function stockState(x){const q=Number(x.stock_qty||0),m=Number(x.minimum_qty||0);if(x.minimum_qty!==null&&q<=m)return'critical';if(x.minimum_qty!==null&&m>0&&q<=m*1.25)return'low';return'normal'}
function renderStock(){const q=$('#stockSearch').value.toLowerCase(),w=$('#stockWarehouse').value||'all',s=$('#stockState').value||'all';const rows=stockDataset().filter(x=>(w==='all'||x.warehouse_id===w)&&(!q||x.product_name.toLowerCase().includes(q))&&(s==='all'||stockState(x)===s));$('#stockList').innerHTML=rows.length?rows.map(x=>{const state=stockState(x),m=Number(x.minimum_qty||0),qv=Number(x.stock_qty||0),ratio=m?Math.min(100,(qv/(m*2))*100):100;return`<div class="row stock-card" data-stock="${x.warehouse_id}|${x.product_id}"><div class="stock-top"><div class="grow"><div class="title">${esc(x.product_name)}</div><div class="subtext">${esc(x.warehouse_name)}${x.minimum_qty!==null?` · mínimo ${fmt(x.minimum_qty)} ${x.base_unit}`:''}</div></div><div style="text-align:right"><div class="stock-qty ${state==='critical'?'low':''}">${fmt(x.stock_qty)} ${x.base_unit}</div><span class="badge ${state==='critical'?'red':state==='low'?'amber':'green'}">${state==='critical'?'CRÍTICO':state==='low'?'BAJO':'NORMAL'}</span></div></div><div class="progress ${state==='critical'?'red':state==='low'?'amber':''}"><i style="width:${ratio}%"></i></div></div>`}).join(''):'<div class="empty">No hay resultados.</div>';$$('[data-stock]').forEach(el=>el.onclick=()=>openStockDetail(...el.dataset.stock.split('|')))}
async function openStockDetail(warehouseId,productId){const p=product(productId),s=stockDataset().find(x=>x.warehouse_id===warehouseId&&x.product_id===productId);openModal(p?.name||'Producto',`${whName(warehouseId)} · ${fmt(s?.stock_qty)} ${p?.base_unit||''}`,'<div class="empty">Cargando lotes…</div>');const r=await query('v_inventory_batch_detail','*',`warehouse_id=eq.${warehouseId}&product_id=eq.${productId}&order=received_at.asc`);const lots=r.data||[];const history=D.moves.filter(m=>(m.movement_lines||[]).some(l=>l.product_id===productId)&&(m.warehouse_from_id===warehouseId||m.warehouse_to_id===warehouseId)).slice(0,8);$('#modalBody').innerHTML=`<div class="grid kpis" style="grid-template-columns:repeat(2,1fr)"><div class="kpi"><div class="label">Stock actual</div><div class="value">${fmt(s?.stock_qty)}</div><div class="meta">${p?.base_unit||''}</div></div><div class="kpi"><div class="label">Lotes abiertos</div><div class="value">${lots.length}</div><div class="meta">Costo real por lote</div></div></div><div class="section-head"><h2>Lotes disponibles</h2></div><div class="list">${lots.map(l=>`<div class="row"><div class="line"><div><div class="title">Lote ${esc(l.lot_reference||('#'+l.movement_no))}</div><div class="subtext">${esc(l.supplier_name||'Sin proveedor')} · ${esc(l.document_number||'Sin documento')} · ${dt(l.received_at)}</div></div><div style="text-align:right"><div class="num">${fmt(l.quantity_remaining)} ${l.base_unit}</div><div class="subtext">${l.unit_cost==null?'Sin costo':money(l.unit_cost,l.currency)+' / '+l.base_unit}</div></div></div></div>`).join('')||'<div class="empty">Sin lotes abiertos.</div>'}</div><div class="section-head"><h2>Historial reciente</h2></div><div class="list">${movementRows(history)}</div>`}
